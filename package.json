{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "test": "lerna run test",
    "ci": "lerna run ci",
    "build": "lerna run build",
    "npm-publish": "npm run test && npm run build && lerna publish"
  }
}