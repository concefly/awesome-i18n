{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "ci": "lerna run ci",
    "build": "lerna run build",
    "npm-publish": "npm run ci && npm run build && lerna publish"
  }
}
