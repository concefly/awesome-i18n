{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.0"
  },
  "scripts": {
    "lerna": "lerna",
    "test": "lerna run test",
    "build": "lerna run build",
    "npm-publish": "npm run test && npm run build && lerna publish"
  }
}
